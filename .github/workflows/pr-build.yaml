name: PR Build
on:
  pull_request:
    branches: ["disabled"]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Init
        uses: ./.github/actions/init

      - name: Check & Build
        run: npm run ci:build
